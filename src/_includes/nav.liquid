
<header>
    <a href="/">
        {% include logo, color:"green" %}
    </a>
    <nav>
        <ul>
            {% for page in nav.pages %}
            <li>
                <a href="{{ page.url }}">{{ page.name }}</a>
            </li>
            {% endfor %}
        </ul>
    </nav>
</header>
